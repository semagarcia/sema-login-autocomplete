(function() {
	/*var port = chrome.runtime.connect();
	console.log("content.js");
	
	$(document).ready(function() {
		// Esto es cada vez que se abre la página que coincide con match (content_script)
		console.log("Ready (content.js)!");
		//$('body').css('background', 'rgb(18, 52, 86)');
	});

	chrome.runtime.onMessage.addListener(function(request, sender, sendResponse) {
  		console.log("onMessagListener: mensaje recibido!");
  	});

	window.addEventListener("message", function(event) {
		console.log("Mensajito! ", event);

		// We only accept messages from ourselves
	  	if (event.source != window)
	    	return;

	  	if (event.data.type && (event.data.type == "FROM_PAGE")) {
	    	console.log("Content script received: " + event.data.text);
	    	port.postMessage(event.data.text);
	  	}
	}, false);*/

})();